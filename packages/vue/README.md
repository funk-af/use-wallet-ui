# @txnlab/use-wallet-ui-vue

Vue UI components for integrating with `@txnlab/use-wallet-vue`.

## Installation

```bash
pnpm add @txnlab/use-wallet-ui-vue @txnlab/use-wallet-vue
```

## Usage

```ts
import { WalletUIProvider, WalletButton } from '@txnlab/use-wallet-ui-vue'
```
